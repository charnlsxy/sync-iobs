# sync-iobs 技术方案

> 状态：**已实现并通过端到端实测**。下文「实测结论」一节为对接口真实探测后的修正，与最初推测不一致的地方以实测为准。

## 一、协议逆向结论（来自 F:\project\loadfile 的 Java 实现）

iobs 不是 S3 标准协议，是自研 HTTP 接口，四个端点全部走同一个自签 token。

### 1. token 生成（Uploader.generateByYourself）

```
scope       = bucket + ":" + key
deadline    = 当前秒级时间戳 + 600          // 10 分钟有效
messageJson = {"scope":"<scope>","deadline":<deadline>}
messageStr  = base64url(messageJson)        // 带 padding，url-safe（- _ =）
skData      = SHA1(secretKey) 的原始 20 字节
signData    = HMAC-SHA1(key = skData, msg = messageStr 的 ASCII 字节)
signStr     = base64url(signData)           // 带 padding
token       = accessKey + ":" + signStr + ":" + messageStr
```

token 作为 URL query 参数原样拼接（含冒号，冒号在 query 中合法）。

代码中留有一组测试向量，可直接用于验证 Rust 实现（凭据为虚构值，仅校验算法）：

```
ak=TESTACCESSKEY000000000000  sk=TESTSECRETKEY000000000000
bucket=test-bucket  key=f14d1f59-767b-404e-93b0-82454786d981  deadline=1732761165
期望 token = TESTACCESSKEY000000000000:iaT60tYaaEAUHZfSgv5qEob40ww=:eyJzY29wZSI6InRlc3QtYnVja2V0OmYxNGQxZjU5LTc2N2ItNDA0ZS05M2IwLTgyNDU0Nzg2ZDk4MSIsImRlYWRsaW5lIjoxNzMyNzYxMTY1fQ==
```

> 算法已与参考 Java 实现逐字节比对通过；上面的向量是把同一算法代入虚构凭据后的等价结果。

### 2. 四个端点

| 用途 | 方法 | 路径 | 请求体 / 说明 |
|---|---|---|---|
| 小文件上传 | POST | `/upload/{bucket}/{key}` | `multipart/form-data`，两个字段：`token`（文本）与 `file`（文件，`filename=` 可自定义后缀）。UA 保持 `csp-iobs-java-sdk/1.3.4` |
| 分片初始化 | POST | `/initUploadPart/{bucket}/{key}?token=&fileSize=&fileName=` | `fileName` 需 percent-encoding。响应体即 uploadId（纯文本） |
| 上传分片 | POST | `/uploadPart/{bucket}/{key}?token=&uploadId=&partNumber=` | 请求体=分片原始字节，`Content-Type: application/octet-stream`；partNumber 从 **1** 开始；响应 JSON 取 `hash` |
| 合并分片 | POST | `/completeUpload/{bucket}/{key}?token=&uploadId=` | `application/x-www-form-urlencoded`，body = `partMap=` + urlencode(`{"1":"hash1","2":"hash2",...}`) |
| 下载 | GET | `/download/{bucket}/{key}?token=` | 常规 GET，响应即文件流 |

### 3. 分片大小分流

Java 逻辑：`file.length() < 10MB` 走小文件直传，否则走分片。阈值做成可配置项。

### 4. Java 实现中已发现的问题（Rust 版会修正）

- `tol = length / chunkSize + 1` 在整除时会多出一个长度为 0 的空分片 → 改为向上取整，不产生空片。
- `raf.read(data, 0, s)` 未处理短读 → 改用 `read_exact`。
- 分片上传未处理 token 10 分钟过期 → 长传场景按剩余有效期自动续签。
- 上传失败无重试 → 分片级重试 + 指数退避。

---

## 二、目标与范围

- 双向同步：`upload`（本地 → iobs）与 `download`（iobs → 本地）。
- 大小文件自动分流，也可 `--force-small` / `--force-multipart` 手动指定。
- 上传/下载实时进度（百分比、已传/总量、速度、ETA）。
- 自定义 key 与服务端文件名后缀（对应 Java 里强制改成 `.png` 的行为）。
- 内外网 base_url 可灵活配置、一条命令切换。
- 本期范围：单文件 + 显式 key（不做目录级 list 双向同步）。

---

## 三、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言/版本 | Rust edition 2021，最新 stable | 已确认目标 Windows 10+，无需锁 1.77.2 |
| 异步 | **不用** tokio/async | 阻塞式同步 IO 足够，依赖更少，避免 async 运行时在老系统上的坑 |
| HTTP | `ureq` 2.x，feature = `native-tls`，关闭默认 rustls | 阻塞式、依赖约 20~30 个 crate；TLS 走系统 Schannel，证书链用系统根证书，无需 OpenSSL 运行库 |
| 签名 | **自实现** SHA1 + HMAC-SHA1 + base64url（约 150 行） | 零依赖，且只用到这一个算法组合，可控可单测 |
| JSON | **自实现** 极简取值器（取 `hash`、`uploadId`）+ 拼接 `partMap` | 只涉及两个字段，不值得为 serde 增加依赖 |
| CLI / INI | 自实现（约 120 行） | 不引入 clap、serde_ini |
| 进度条 | 自实现，`\r` 单行重绘 | 无依赖；非 TTY 时自动降级为按百分比打点，便于重定向日志 |

**最终依赖：仅 `ureq` + `native-tls`（及其传递依赖）。**

> 说明：本机当前没有 Rust 工具链（`~/.cargo` 不存在），实现阶段需要先装 rustup（默认 MSVC 工具链）。

---

## 四、模块划分（约 900~1100 行）

```
src/
  main.rs       CLI 解析、子命令分发、退出码
  config.rs     INI 解析 + 优先级合并（命令行 > 环境变量 > 配置文件 > 默认）
  token.rs      SHA1 / HMAC-SHA1 / base64url → token（含单测，用上面的向量对拍）
  iobs.rs       四个端点的 URL 拼装与调用：upload_small / init / part / complete / download
  http.rs       ureq 封装：超时、重试、--insecure、流式 body、进度回调注入
  progress.rs   进度条渲染（百分比 / 速度 / ETA）
```

---

## 五、关键流程

### 上传
1. 读文件元信息，与 `small_file_limit` 比较分流。
2. **小文件**：按 multipart 边界拼出「头部 + 文件流 + 尾部」的链式 Reader，直接流式发送 —— 不把文件整包读进内存，且能在读取时回调进度。
3. **大文件**：
   - `initUploadPart` 取 uploadId（响应兼容纯文本与 JSON 两种形态）；
   - 按 `chunk_size`（默认 5MB）逐片 `read_exact` 后发送，收集每片返回的 `hash`；
   - 单片失败重试（默认 3 次，指数退避），token 临期自动重签；
   - `completeUpload` 提交 `partMap`。
4. 输出最终下载 URL。

### 下载
1. 拼下载 URL 并发 GET；`Content-Length` 取总大小。
2. 流式写入目标文件，边写边回调进度。
3. `--resume` 走 Range 续传：先写 `.part` 临时文件，完成后重命名。

### 进度显示
`[##########----------] 52.3%  12.4MB/23.7MB  3.2MB/s  eta 4s`
非 TTY 时改为每 5% 输出一行，避免 `\r` 把日志冲掉。

---

## 六、配置设计（iobs.ini）

```ini
[common]
bucket            = your-bucket           ; 按实际填写
access_key        =                      ; 按实际填写
secret_key        =                      ; 按实际填写
small_file_limit  = 10485760     ; 10MB，超过则分片
chunk_size        = 5242880      ; 5MB
timeout           = 60           ; 秒
retry             = 3
token_ttl         = 600          ; 秒

[outer]                                  ; 环境段示例，名字可自由增删
base_url = https://iobs.example.com

[inner]
base_url = https://iobs-inner.example.com
```

查找顺序：`--config` 指定 > 当前目录 `iobs.ini` > exe 同目录 `iobs.ini` > `%APPDATA%\sync-iobs\iobs.ini`。

环境变量覆盖：`IOBS_AK` / `IOBS_SK` / `IOBS_BUCKET` / `IOBS_BASE_URL` / `IOBS_ENV`。

`--env` 可取任意 section 名，不限于 outer/inner，需要几个环境就加几个 section。

> `base_url` / `bucket` / `access_key` / `secret_key` **均不内置默认值**，缺任一项都会在启动时明确报错。

---

## 七、CLI 设计

```bash
# 上传（key 用文件名，后缀保持原样）
sync-iobs upload ./a.zip

# 自定义 key 与服务端文件名后缀（--ext 只改 multipart 的 filename，不改 key）
sync-iobs upload ./a.zip --key cz20251130_test --ext png

# 强制分片 / 指定分片大小
sync-iobs upload ./big.iso --chunk 10MB --force-multipart

# 下载
sync-iobs download cz20251130_test --out ./recv/a.zip --resume

# 只打印带 token 的下载 URL（便于对拍调试）
sync-iobs url cz20251130_test

# 环境切换
sync-iobs upload ./a.zip --env inner
sync-iobs download cz20251130_test --base-url https://iobs.example.com
```

全局选项：`-c/--config`、`--env`、`--base-url`、`--bucket`、`--ak`、`--sk`、`--timeout`、`--insecure`（跳过证书校验）、`--no-progress`、`-v/--verbose`（打印完整请求/响应，便于和 Java 对拍）。

key 默认规则：取本地文件名（不含目录）；`--key` 显式覆盖；支持占位符 `{name}` `{ext}` `{ts}` `{uuid}`。

---

## 八、可靠性与兼容

- 分片级重试 + 指数退避；合并失败保留 uploadId 便于排查。
- token 按剩余有效期自动续签（长传刚需）。
- 退出码：0 成功 / 2 参数错误 / 3 网络错误 / 4 服务端非 2xx / 5 IO 错误。
- 编译：`x86_64-pc-windows-msvc`，开启 `-C target-feature=+crt-static` 静态链 CRT，目标机器免装 VC 运行库。
- `--insecure` 作为内网自签证书场景的逃生口（native-tls/Schannel 不便注入自定义 CA）。

---

## 九、验收方式

1. `cargo test`：用 Java 注释里的 token 向量校验签名实现，逐字节一致。
2. `sync-iobs url <key>` 生成的 URL 与 Java 打印的 URL 字符串对比（token 段除外，deadline 随时间变）。
3. `-v` 打印请求/响应，与 Java 实际请求逐字段对拍。
4. 实测：小文件（<10MB）直传、大文件（>10MB）分片、下载、断点续传。

---

## 十、实测结论（对上面推测的修正）

用 Java 里的 ak/sk 对真实接口探测后的结果：

1. **`initUploadPart` 响应确实是纯文本**，是 48 位十六进制字符串（不是 JSON、也不是 UUID），
   例如 `ba655534ee33ae9ad1a4a56b4044239ab854926fc3da0530`。代码仍保留 JSON 兼容解析。
2. **`uploadPart` / 小文件 `upload` 响应**：`{"bucket":"...","hash":"<32位hex>","key":"..."}`。
3. **`completeUpload` 响应**：同构 JSON，hash 形如 `<hash>-1`（后缀为分片数）。
4. **下载**：`GET /download/...` 返回 200，直接是文件流，支持 Range（206）。
5. **分片大小下限就是 5MB**：实测 128KB / 1MB / 2MB / 3MB / 4MB 均返回
   `400 {"code":400,"error":"EntityTooSmall"}`，5MB 正常。
   程序在检测到 `chunk_size < 5MB` 时打印警告并自动纠正为 5MB。
6. **bucket 会拒绝部分文件后缀**：传 `.bin` 时返回
   `402 {"code":402,"info":"The bucket <bucket> refused bin file type"}`。
   这就是「自定义文件名后缀」是刚需的原因 —— 已支持 `--ext` 与配置 `file_ext`。
7. **签名算法已用一组 token 向量逐字节校验通过**（`cargo test` 中的
   `matches_reference_token`）。
8. **环境地址需按实际网络配置**：不内置任何地址，多环境用 `[section]` + `--env` 切换。
9. 服务版本：`IOBS Service / 2.13.0`（Powered by PACloud）。

## 十一、端到端实测记录

| 场景 | 结果 |
|---|---|
| 小文件直传 512KB → 下载回环 | md5 一致 |
| 大文件分片 25MB（5 片×5MB）→ 下载回环 | md5 一致 |
| 强制分片 + 占位符 key + `--ext` | 通过 |
| 断点续传（已有 10MB，续到 25MB） | md5 一致 |
| `url` 子命令 | 正常输出带 token 的下载地址 |

## 十二、图形界面（egui）

在 CLI 之上加了一层原生 GUI：

- 框架 `eframe/egui 0.36` + `rfd`（系统文件/目录选择框），做成 **可选 feature**，默认开启；
  `cargo build --release --no-default-features` 仍可编出只依赖 ureq + native-tls 的纯 CLI 版（0.7MB）
- 入口：无参数启动 / `sync-iobs gui` → GUI；带子命令 → CLI（两者共用同一套配置与同步逻辑）
- 任务在后台 `std::thread` 执行，进度通过 `Arc<Mutex<Vec<TaskState>>>` 回传（`Progress::for_shared`），
  UI 每帧读取；有任务在跑时 `request_repaint_after(100ms)` 保证进度条平滑
- 中文字体：egui 内置字体不含中文，启动时按 `simhei.ttf → Deng.ttf → msyh.ttc → simsun.ttc`
  顺序加载系统字体，否则中文全是方块
- GUI 模式下调用 `ShowWindow(GetConsoleWindow(), SW_HIDE)` 隐藏控制台黑框

产物：`target/release/sync-iobs.exe`，约 8.4MB（MSVC 工具链编译）。

## 十三、遗留事项

1. 内网 base_url 待在内网环境验证连通性。
2. 目录级双向同步（需要 list 接口）本期未做，仍为单文件 + 显式 key。
3. 服务端允许的后缀白名单没有接口可查，目前靠 `file_ext` 配置规避。
